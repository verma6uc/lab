import React from 'react';
import { Card, CardContent, Typography } from '@mui/material';

const UIArchetypes = () => {
  return (
    <Card>
      <CardContent>
        <Typography variant="h6">UI Archetypes</Typography>
        {/* Content will be implemented later */}
      </CardContent>
    </Card>
  );
};

export default UIArchetypes; 